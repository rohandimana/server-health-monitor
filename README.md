# Server Health Monitor

This Python script checks CPU, RAM, and Disk usage
and saves the output in a log file.

## Requirements
- Python 3
- psutil library

## Usage
pip install psutil
python server_health.py
